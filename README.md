# Homebrew tap for files-age

Install the public files-age release with:

```sh
brew install chengyixu/files-age/files-age
```

The formula downloads the immutable v1.0.3 GitHub Release asset and verifies its SHA-256 checksum before installation.
